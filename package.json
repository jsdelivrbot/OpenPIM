{
  "name": "static-server",
  "version": "1.0.0",
  "description": "A static files server",
  "main": "server.js",
  "scripts": {
    "install": "bower install && (cd bower_components/forge && npm install && npm run bundle) && gulp",
    "clean": "rm -r bower_components node_modules",
    "watch": "gulp watch"
  },
  "author": "Nuno Santos",
  "license": "MIT",
  "dependencies": {
    "express": "^4.13.4"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-concat": "^2.6.0",
    "gulp-sourcemaps": "^1.6.0",
    "gulp-uglify": "^1.5.1",
    "gulp-util": "^3.0.7"
  }
}
